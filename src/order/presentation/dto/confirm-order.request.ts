/**
 * Confirm order request.
 * The orderId is extracted from the route parameter (:id).
 * No additional body is required.
 */
export class ConfirmOrderRequest {
  // Intentionally empty — orderId comes from URL params
}
